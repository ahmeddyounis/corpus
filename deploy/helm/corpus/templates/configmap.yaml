apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "corpus.fullname" . }}-config
  labels:
    {{- include "corpus.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
